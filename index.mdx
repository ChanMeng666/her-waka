---
title: HER WAKA
description: "HER WAKA — Navigating pathways into sustainable employment. Employment-focused workshops for women in Auckland, delivered by SheSharp for MSD clients."
mode: custom
---

<style>{`
  @import url('https://fonts.googleapis.com/css2?family=Anton&family=Inter:wght@400;500;600&family=Playfair+Display:wght@700;800;900&display=swap');

  #px-sidebar {
    width: 260px;
    min-width: 260px;
    background: #0a0920;
    position: relative;
    z-index: 10;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0 24px;
    border-left: 1px solid rgba(255,255,255,0.06);
  }
  #px-sidebar a { cursor: pointer !important; }

  @keyframes bounceHint {
    0%,100% { transform: translateX(-50%) translateY(0); opacity: 0.4; }
    50%     { transform: translateX(-50%) translateY(8px); opacity: 0.8; }
  }

  @media (max-width: 768px) {
    #px-sidebar { display: none !important; }
  }

  /* Hide Mintlify default content wrapper padding */
  .px-content-wrapper { padding: 0 !important; margin: 0 !important; max-width: 100% !important; }
`}</style>

<h1 style={{ position: 'absolute', width: '1px', height: '1px', overflow: 'hidden', clip: 'rect(0,0,0,0)', whiteSpace: 'nowrap' }}>
  HER WAKA — Navigating pathways into sustainable employment
</h1>

<div id="slider-root" style={{ width: '100vw', marginLeft: 'calc(-50vw + 50%)', overflow: 'hidden', background: '#0a0920', position: 'relative' }}></div>

<div style={{ display: 'none' }} dangerouslySetInnerHTML={{ __html: `
<textarea id="px-code" style="display:none">
(function(){
  var fP="'Playfair Display',Georgia,serif";
  var fA="'Anton',sans-serif";
  var fI="'Inter',-apple-system,sans-serif";
  var root = document.getElementById('slider-root');
  if(!root) return;

  var rect = root.getBoundingClientRect();
  var topOffset = rect.top;
  var sH = window.innerHeight - topOffset;
  root.style.height = sH + 'px';

  var DATA = [
    { type:'text', title:'HER WAKA', sub:'Programme', yr:'2026',
      desc:'Navigating pathways into sustainable employment',
      img:'/images/banners/her-waka-banner-landscape.png', pos:'center 85%',
      ov:'linear-gradient(135deg, rgba(31,30,68,0.88) 0%, rgba(45,27,78,0.85) 50%, rgba(155,46,131,0.82) 100%)',
      over:'March \\u2013 June 2026 \\u00b7 Auckland',
      main:'HER WAKA', tag:'Navigating pathways into sustainable employment' },
    { type:'text', title:'AI & Future of Work', sub:'Next Session', yr:'25 MAR',
      desc:'Panel discussion, hands-on AI workshop, hiring insights',
      img:'/images/banners/slide2-womens-day.jpg', pos:'center 30%',
      ov:'linear-gradient(135deg, rgba(12,28,38,0.9) 0%, rgba(31,30,68,0.88) 50%, rgba(61,31,92,0.85) 100%)',
      over:'Next Session \\u00b7 25 March 2026',
      main:'AI & Future of Work', tag:'Panel, hands-on workshop, tech hiring insights' },
    { type:'photo', title:'Chan Meng', sub:'Featured Speaker', yr:'Gavigo',
      desc:'Senior AI/ML Engineer \\u00b7 UN CSW 69 speaker',
      img:'/images/banners/slide3-shesharp-entrance.jpg', pos:'center center',
      ov:'rgba(31,30,68,0.2)' },
    { type:'text', title:'SheSharp', sub:'Delivered By', yr:'Est. 2014',
      desc:'Supporting women in technology across Aotearoa',
      img:'/images/banners/her-waka-banner-portrait.png', pos:'center 65%',
      ov:'linear-gradient(135deg, rgba(45,27,78,0.88) 0%, rgba(200,70,171,0.82) 60%, rgba(137,130,255,0.85) 100%)',
      over:'Delivered By', main:'SheSharp',
      tag:'Supporting women in technology across Aotearoa New Zealand' },
    { type:'cta', title:'Register', sub:'Join Us', yr:'Auckland',
      desc:'25 March 2026 \\u00b7 12\\u20132 pm \\u00b7 academyEX Pikopiko',
      img:'/images/banners/her-waka-banner-landscape.png', pos:'center 50%',
      ov:'linear-gradient(135deg, rgba(155,46,131,0.85) 0%, rgba(200,70,171,0.8) 40%, rgba(31,30,68,0.88) 100%)',
      over:'25 March 2026 \\u00b7 Auckland',
      main:'Register Now', tag:'academyEX Pikopiko \\u00b7 12\\u20132 pm' }
  ];
  var N=DATA.length, BUF=4, LERP=0.05, SPD=0.75, MAXV=150, SNAP=500;
  var curY=0, tgtY=0, isDrag=false, isSnap=false;
  var ss={t:0,y:0,g:0}, lastT=Date.now(), ds={y:0,s:0};
  var mH=160;
  var vMin=-BUF, vMax=BUF;

  function gd(i){ return DATA[((i%N)+N)%N]; }
  function gn(i){ return String(((Math.abs(i)%N)+N)%N+1).padStart(2,'0'); }

  root.innerHTML='';
  root.style.cssText='height:'+sH+'px;overflow:hidden;position:relative;background:#0a0920;display:flex;cursor:grab;user-select:none;-webkit-user-select:none;width:100vw;';

  var mainEl=document.createElement('div');
  mainEl.style.cssText='flex:1;position:relative;overflow:hidden;';
  root.appendChild(mainEl);

  var side=document.createElement('div');
  side.id='px-sidebar';
  root.appendChild(side);

  var mmBox=document.createElement('div');
  mmBox.style.cssText='width:100%;height:'+mH+'px;overflow:hidden;border-radius:6px;position:relative;margin-bottom:20px;';
  side.appendChild(mmBox);

  var infoBox=document.createElement('div');
  infoBox.style.cssText='position:relative;height:'+mH+'px;overflow:hidden;';
  side.appendChild(infoBox);

  var hint=document.createElement('div');
  hint.style.cssText='position:absolute;bottom:28px;left:50%;z-index:20;text-align:center;pointer-events:none;animation:bounceHint 2s ease infinite;';
  hint.innerHTML='<p style="font-family:'+fI+';font-size:11px;letter-spacing:0.12em;text-transform:uppercase;color:rgba(255,255,255,0.3);margin:0 0 6px;">Scroll</p><div style="width:1px;height:20px;background:rgba(255,255,255,0.2);margin:0 auto;"></div>';
  mainEl.appendChild(hint);

  var sEls={},mEls={},iEls={},pxS={};

  function mkSlide(i){
    var d=gd(i), el=document.createElement('div');
    el.style.cssText='position:absolute;top:0;left:0;width:100%;height:'+sH+'px;overflow:hidden;';

    var bg=document.createElement('div');
    bg.style.cssText='position:absolute;inset:-20%;will-change:transform;';

    if(d.img){
      var im=document.createElement('img');
      im.src=d.img; im.alt=d.title; im.draggable=false;
      im.style.cssText='width:100%;height:100%;object-fit:cover;object-position:'+(d.pos||'center center')+';display:block;';
      bg.appendChild(im);
      el.appendChild(bg); el._bg=bg;

      if(d.ov){
        var ov=document.createElement('div');
        ov.style.cssText='position:absolute;inset:0;background:'+d.ov+';';
        el.appendChild(ov);
      }
    } else {
      bg.style.cssText='position:absolute;inset:-20%;will-change:transform;background:'+d.bg+';background-size:200% 200%;';
      el.appendChild(bg); el._bg=bg;
    }

    var nm=document.createElement('div');
    nm.textContent=gn(i);
    nm.style.cssText='position:absolute;bottom:36px;left:40px;font-family:'+fP+';font-size:100px;font-weight:800;color:rgba(255,255,255,0.025);line-height:1;pointer-events:none;';
    el.appendChild(nm);

    if(d.type==='text'){
      var w=document.createElement('div');
      w.style.cssText='position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;z-index:2;padding:48px 56px;text-align:center;';
      w.innerHTML=
        '<p style="font-family:'+fI+';font-size:11px;font-weight:500;letter-spacing:0.18em;color:rgba(255,255,255,0.35);margin:0 0 20px;text-transform:uppercase;">'+(d.over||'')+'</p>'+
        '<h2 style="font-family:'+fP+';font-size:clamp(44px,7vw,82px);font-weight:800;color:white;letter-spacing:-0.02em;line-height:1.0;text-shadow:0 2px 20px rgba(0,0,0,0.2);margin:0 0 16px;">'+d.main+'</h2>'+
        '<div style="width:40px;height:1px;background:rgba(255,255,255,0.15);margin:0 auto 16px;"></div>'+
        '<p style="font-family:'+fA+';font-size:15px;color:rgba(255,255,255,0.5);margin:0;max-width:420px;line-height:1.5;text-transform:uppercase;letter-spacing:0.02em;">'+(d.tag||'')+'</p>';
      el.appendChild(w);
    }
    else if(d.type==='cta'){
      var w=document.createElement('div');
      w.style.cssText='position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;z-index:3;padding:48px 56px;text-align:center;';
      var ctaId='px-cta-'+i;
      w.innerHTML=
        '<p style="font-family:'+fI+';font-size:11px;font-weight:500;letter-spacing:0.18em;color:rgba(255,255,255,0.38);margin:0 0 20px;text-transform:uppercase;">'+(d.over||'')+'</p>'+
        '<h2 style="font-family:'+fP+';font-size:clamp(40px,7vw,72px);font-weight:800;color:white;letter-spacing:-0.02em;line-height:1.0;text-shadow:0 2px 20px rgba(0,0,0,0.2);margin:0 0 14px;">'+d.main+'</h2>'+
        '<p style="font-family:'+fA+';font-size:15px;color:rgba(255,255,255,0.5);margin:0 0 34px;text-transform:uppercase;letter-spacing:0.02em;">'+(d.tag||'')+'</p>'+
        '<a id="'+ctaId+'" href="https://events.humanitix.com/she-sharp-ministry-of-social-development-academyex-her-waka" '+
          'style="font-family:'+fI+';display:inline-block;background:white;color:#9b2e83;padding:14px 40px;border-radius:6px;font-weight:600;font-size:15px;text-decoration:none;box-shadow:0 2px 20px rgba(200,70,171,0.25);cursor:pointer;transition:transform 0.3s;"'+
        '>Register on Humanitix</a>';
      el.appendChild(w);
      var btn=w.querySelector('#'+ctaId);
      if(btn){
        btn.addEventListener('mouseover',function(){ this.style.transform='scale(1.05)'; });
        btn.addEventListener('mouseout',function(){ this.style.transform='scale(1)'; });
      }
    }
    return el;
  }

  function mkMm(i){
    var d=gd(i), el=document.createElement('div');
    el.style.cssText='position:absolute;top:0;left:0;width:100%;height:'+mH+'px;overflow:hidden;will-change:transform;border-radius:4px;';
    if(d.img){
      var im=document.createElement('img');
      im.src=d.img; im.alt=''; im.draggable=false;
      im.style.cssText='width:100%;height:100%;object-fit:cover;object-position:'+(d.pos||'center center')+';display:block;';
      el.appendChild(im);
      if(d.ov){
        var ov=document.createElement('div');
        ov.style.cssText='position:absolute;inset:0;background:'+d.ov+';opacity:0.4;';
        el.appendChild(ov);
      }
    } else {
      el.style.background=d.bg;
    }
    return el;
  }

  function mkInfo(i){
    var d=gd(i), el=document.createElement('div');
    el.style.cssText='position:absolute;top:0;left:0;width:100%;height:'+mH+'px;will-change:transform;display:flex;flex-direction:column;justify-content:center;';
    el.innerHTML=
      '<p style="font-family:'+fI+';font-size:12px;font-weight:500;letter-spacing:0.15em;color:rgba(255,255,255,0.25);margin:0 0 4px;font-variant-numeric:tabular-nums;">'+gn(i)+'</p>'+
      '<p style="font-family:'+fP+';font-size:16px;font-weight:700;color:white;margin:0 0 8px;letter-spacing:-0.01em;">'+d.title+'</p>'+
      '<div style="width:36px;height:1px;background:rgba(255,255,255,0.1);margin:0 0 8px;"></div>'+
      '<div style="display:flex;justify-content:space-between;margin:0 0 4px;">'+
        '<p style="font-family:'+fI+';font-size:10px;font-weight:500;color:rgba(255,255,255,0.28);letter-spacing:0.1em;text-transform:uppercase;margin:0;">'+d.sub+'</p>'+
        '<p style="font-family:'+fI+';font-size:10px;font-weight:500;color:rgba(255,255,255,0.28);letter-spacing:0.1em;margin:0;">'+d.yr+'</p>'+
      '</div>'+
      '<p style="font-family:'+fI+';font-size:11px;font-weight:400;color:rgba(255,255,255,0.38);line-height:1.5;margin:0;">'+d.desc+'</p>';
    return el;
  }

  function syncRange(mn,mx){
    var k;
    for(k in sEls) if(+k<mn||+k>mx){ sEls[k].parentNode&&sEls[k].parentNode.removeChild(sEls[k]); delete sEls[k]; delete pxS[k]; }
    for(k in mEls) if(+k<mn||+k>mx){ mEls[k].parentNode&&mEls[k].parentNode.removeChild(mEls[k]); delete mEls[k]; }
    for(k in iEls) if(+k<mn||+k>mx){ iEls[k].parentNode&&iEls[k].parentNode.removeChild(iEls[k]); delete iEls[k]; }
    for(var i=mn;i<=mx;i++){
      if(!sEls[i]){ sEls[i]=mkSlide(i); mainEl.appendChild(sEls[i]); pxS[i]=0; }
      if(!mEls[i]){ mEls[i]=mkMm(i); mmBox.appendChild(mEls[i]); }
      if(!iEls[i]){ iEls[i]=mkInfo(i); infoBox.appendChild(iEls[i]); }
    }
  }

  function updatePos(){
    var mmY=(curY*mH)/sH, k,i,el,bg,y,t;
    for(k in sEls){
      i=+k; el=sEls[k]; bg=el._bg;
      y=i*sH+curY;
      el.style.transform='translateY('+y+'px)';
      if(bg){
        t=(-curY-i*sH)*0.15;
        pxS[i]=(pxS[i]||0)+(t-(pxS[i]||0))*0.1;
        bg.style.transform='translateY('+pxS[i].toFixed(1)+'px) scale(1.3)';
      }
    }
    for(k in mEls){ mEls[k].style.transform='translateY('+(+k*mH+mmY)+'px)'; }
    for(k in iEls){ iEls[k].style.transform='translateY('+(+k*mH+mmY)+'px)'; }
  }

  function doSnap(){
    isSnap=true;
    ss={t:Date.now(),y:tgtY,g:-Math.round(-tgtY/sH)*sH};
  }
  function tickSnap(){
    var p=Math.min((Date.now()-ss.t)/SNAP,1);
    tgtY=ss.y+(ss.g-ss.y)*(1-Math.pow(1-p,3));
    if(p>=1) isSnap=false;
  }

  var hintGone=false;
  function animate(){
    var now=Date.now();
    if(!isSnap&&!isDrag&&now-lastT>100){
      var sp=-Math.round(-tgtY/sH)*sH;
      if(Math.abs(tgtY-sp)>1) doSnap();
    }
    if(isSnap) tickSnap();
    if(!isDrag) curY+=(tgtY-curY)*LERP;

    var ci=Math.round(-tgtY/sH);
    var mn=ci-BUF, mx=ci+BUF;
    if(mn!==vMin||mx!==vMax){ vMin=mn; vMax=mx; syncRange(mn,mx); }
    updatePos();

    if(!hintGone&&Math.abs(tgtY)>10){
      hintGone=true;
      hint.style.opacity='0'; hint.style.transition='opacity 0.5s';
      setTimeout(function(){ hint.parentNode&&hint.parentNode.removeChild(hint); },600);
    }
    requestAnimationFrame(animate);
  }

  root.addEventListener('wheel',function(e){
    e.preventDefault(); isSnap=false; lastT=Date.now();
    tgtY-=Math.max(Math.min(e.deltaY*SPD,MAXV),-MAXV);
  },{passive:false});

  root.addEventListener('touchstart',function(e){
    isDrag=true; isSnap=false;
    ds={y:e.touches[0].clientY,s:tgtY}; lastT=Date.now();
  });
  root.addEventListener('touchmove',function(e){
    if(!isDrag)return;
    tgtY=ds.s+(e.touches[0].clientY-ds.y)*1.5; lastT=Date.now();
  });
  root.addEventListener('touchend',function(){ isDrag=false; });
  window.addEventListener('resize',function(){
    var r=root.getBoundingClientRect();
    sH=window.innerHeight-r.top;
    root.style.height=sH+'px';
  });

  syncRange(vMin,vMax);
  requestAnimationFrame(animate);
})();
</textarea>
<img src="data:," onerror="new Function(document.getElementById('px-code').value)()" />
`}} />
